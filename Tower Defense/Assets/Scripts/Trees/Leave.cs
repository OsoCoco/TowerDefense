using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Leave : MonoBehaviour
{
 /*   [HideInInspector]
    public Leave myFather;
    [Header("Atributes")]
    public bool isAdan;
    public bool isTheChosenOne = false;
    public List<Leave> myChilds;
    public int myGen;

    private void Start()
    {
        TreeCreator.instance.leaves.Add(this);
        if (isAdan == false)
        {
            TreeCreator.instance.CreateTree(this);
        }
    }
 */
}
